-- ============================================================
-- Mushketon Live Scoring — смена токена судьи
-- Supabase → SQL Editor → New Query → вставить → заменить токен → Run
-- ============================================================
-- Замени 'НОВЫЙ_ТОКЕН' на новую строку (например 'mushketon2026').
-- После выполнения:
--   • старая ссылка судьи (?token=СТАРЫЙ) перестаёт писать в базу
--   • отправь судье новую ссылку: .../judge/?token=НОВЫЙ_ТОКЕН
-- Код репозитория при этом НЕ меняется.
-- ============================================================
CREATE OR REPLACE FUNCTION get_judge_token()
RETURNS text
LANGUAGE sql
SECURITY DEFINER
STABLE
AS $$
  SELECT 'НОВЫЙ_ТОКЕН';
$$;
